{
  "name": "stage-gen",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "pipeline",
    "web"
  ],
  "scripts": {
    "pipeline": "bun run pipeline/src/cli.ts",
    "web": "bun --filter @stage-gen/web dev",
    "web:build": "bun --filter @stage-gen/web build"
  },
  "devDependencies": {
    "@types/bun": "^1.1.14",
    "pngjs": "^7.0.0",
    "puppeteer": "^24.42.0",
    "typescript": "^5.6.3"
  }
}
