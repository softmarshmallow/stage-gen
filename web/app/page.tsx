export default function Home() {
  return (
    <main style={{ padding: 32, maxWidth: 720, margin: "0 auto" }}>
      <h1 style={{ fontSize: 18, fontWeight: 600 }}>stage-gen</h1>
      <p style={{ color: "#666" }}>
        Phase 0 placeholder. The picker UI lands in a later phase.
      </p>
      <p style={{ color: "#666" }}>
        Loading affordance is committed at <code>/loading.gif</code>.
      </p>
    </main>
  );
}
