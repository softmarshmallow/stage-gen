[build-system]
requires = ["hatchling>=1.27,<2"]
build-backend = "hatchling.build"

[project]
name = "bellweather-pipeline"
version = "0.0.0"
description = "Preparation implementation for bellweather pipeline"
requires-python = ">=3.12"
dependencies = ["stage-gen", "demo-game-tools"]

[tool.hatch.build.targets.wheel]
packages = ["src/bellweather_pipeline"]

[tool.uv.sources]
stage-gen = { workspace = true }
demo-game-tools = { workspace = true }
